
plugins {
    id 'org.ajoberstar.git-publish' version '0.3.0'
}

gitPublish {
    repoUri = 'git@github.com:cjstehno/coffeaelectronica.git'
    branch = 'gh-pages'

    contents {
        from(file('build/jbake')) {
            into '.'
        }
    }
}
